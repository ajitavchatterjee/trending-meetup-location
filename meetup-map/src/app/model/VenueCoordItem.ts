export class VenueCoordItem {
    lat: number;
    lon: number;
    weight: number;
    constructor(lat: number, lon: number, weight: number) {
      this.lat = lat;
      this.lon = lon;
      this.weight = weight;
    }
  }